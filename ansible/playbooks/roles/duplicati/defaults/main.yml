---
duplicati_version: "latest"
duplicati_dir: "/opt/duplicati"
duplicati_puid: "1000"
duplicati_pgid: "1000"
duplicati_timezone: "Etc/UTC"
duplicati_settings_key: "{{ lookup('ansible.builtin.env', 'DUPLICATI_SETTINGS_KEY') }}"
duplicati_port: 8200
duplicati_web_password: "{{ lookup('ansible.builtin.env', 'DUPLICATI_WEB_PASSWORD') }}"
