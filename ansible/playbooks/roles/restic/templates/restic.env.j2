# Managed by Ansible. Values are references, not secret material.
RESTIC_REPOSITORY={{ restic_repository | quote }}
RESTIC_PASSWORD_FILE={{ restic_password_file | quote }}
RCLONE_CONFIG={{ restic_rclone_config | quote }}
RESTIC_CACHE_DIR={{ restic_cache_dir | quote }}
RESTIC_HOST={{ restic_backup_host | quote }}
