services:
  prometheus:
    image: prom/prometheus:{{ prometheus_version }}
    restart: unless-stopped
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
      - prometheus_data:/prometheus
    ports:
      - "{{ prometheus_bind_address }}:9090:9090"
volumes:
  prometheus_data:
